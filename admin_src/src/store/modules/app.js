const state = {
    name: 'xy cms',
    site: 'zh',
    logo: require('@/assets/images/logo.svg')
}

const mutations = {}

const actions = {}

export default {
    namespaced: true,
    state,
    mutations,
    actions
}
